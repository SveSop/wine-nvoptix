name: Artifacts (Package)

on:
  push:
    branches: [ master ]
    tags: [ v* ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build-artifacts:
    runs-on: ubuntu-24.04

    steps:
    - name: Checkout code
      uses: actions/checkout@v6
      with:
        submodules: recursive
        fetch-depth: 0

    - name: Prepare environment (none-tag)
      if: github.ref_type != 'tag'
      run: |
        export VERSION="${GITHUB_REF##*/}-${GITHUB_SHA::7}"
        echo "VERSION=${VERSION}" >> $GITHUB_ENV
        echo "ARTIFACT=build/nvoptix-${VERSION}" >> $GITHUB_ENV

    - name: Prepare environment (tag)
      if: github.ref_type == 'tag'
      run: |
        export VERSION=$(git describe --always --tags --dirty=+)
        echo "VERSION=${VERSION}" >> $GITHUB_ENV
        echo "ARTIFACT=build/nvoptix-${VERSION}" >> $GITHUB_ENV

    - name: Setup problem matcher
      uses: ammaraskar/gcc-problem-matcher@master

    - name: Build (default)
      uses: SveSop/ubuntu-mingw-github-action@v3
      with:
        command: |
          ./package-release.sh "${{ env.VERSION }}" build

    - name: Build (fakedll)
      uses: SveSop/ubuntu-mingw-github-action@v3
      with:
        command: |
          ./package-release.sh "${{ env.VERSION }}-fakedll" build --fakedll

    - name: Upload artifacts (default)
      uses: actions/upload-artifact@v6
      with:
        name: nvoptix-${{ env.VERSION }}
        path: ${{ env.ARTIFACT }}
        if-no-files-found: error

    - name: Upload artifacts (fakedll)
      uses: actions/upload-artifact@v6
      with:
        name: nvoptix-${{ env.VERSION }}-fakedll
        path: ${{ env.ARTIFACT }}-fakedll
        if-no-files-found: error

    - name: Create release archives
      if: github.ref_type == 'tag'
      run: |
        tar -cJf nvoptix-${{ env.VERSION }}.tar.xz -C build nvoptix-${{ env.VERSION }}
        tar -cJf nvoptix-${{ env.VERSION }}-fakedll.tar.xz -C build nvoptix-${{ env.VERSION }}-fakedll

    - name: Upload assets to Release
      if: github.ref_type == 'tag'
      uses: softprops/action-gh-release@v2
      with:
        tag_name: ${{ github.ref_name }}
        files: |
          nvoptix-${{ env.VERSION }}.tar.xz
          nvoptix-${{ env.VERSION }}-fakedll.tar.xz
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
